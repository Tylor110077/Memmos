# Local-only fallback for Aliyun Bailian API key
#
# Preferred usage:
# export ALIYUN_BAILIAN_API_KEY=your_api_key
#
# If you want to use this file instead, replace the placeholder below with the real key.
# Keep only one non-comment line.

<replace-with-your-api-key>
